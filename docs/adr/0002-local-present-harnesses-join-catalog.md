# Local-present Harnesses join the catalog

The popularity bar stays the default intake. A Harness that is already On Path or Configured on this machine still gets a catalog row, tagged `local-only`, because TokenScope exists to read this machine. Zero (Gitlawb/zero) is the first such row: absent from Terminal Trove and below the star floor, but installed here.
