# Coverage inventory is a research artifact

TokenScope's product surface stays the existing adapters, detect-only sources, and loopback dashboard. The popular-harness catalog, local On Path / Configured / Usage logs scan, and coverage matrix live in `docs/harness-coverage.md` and `scripts/scan-local-harnesses.mjs` so a future reader does not treat that scan as part of `src/indexer.js`.
